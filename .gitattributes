/mvnw text eol=lf
*.cmd text eol=crlf

# Set the default behavior to automatically normalize line endings.
* text=auto

# Explicitly declare text files you want to always have normalized (LF)
*.java text
*.xml text
*.properties text
*.cmd text
*.sh text eol=lf
